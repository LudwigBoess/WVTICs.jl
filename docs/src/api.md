```@meta
CurrentModule = WVTICs
```

# API

[`make_sph_wvtics`](@ref) is the only exported symbol. Everything below is
internal — reach it via `WVTICs.name` or `using WVTICs: name`. The reference
lists every type, constant and function that carries a docstring: the driver
stages, the parameter/kernel types, the problem registry, the SPH/WVT
internals, and the distributed helpers.

## Index

```@index
```

## Reference

```@autodocs
Modules = [WVTICs]
```
```
